import PreferencesWorkspace from '@/components/nemt/PreferencesWorkspace';

export const metadata = {
  title: 'Preferences'
};

const PreferencesPage = () => {
  return <PreferencesWorkspace />;
};

export default PreferencesPage;