import PageMemoryWorkspace from '@/components/nemt/PageMemoryWorkspace';

export const metadata = {
  title: 'Page Memory Profiler'
};

const PageMemoryPage = () => {
  return <PageMemoryWorkspace />;
};

export default PageMemoryPage;